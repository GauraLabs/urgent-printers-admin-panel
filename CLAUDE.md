@AGENTS.md

# Urgent Printers — Admin Panel

Production admin panel for an online printing business. Standalone Next.js 16 app that talks to a FastAPI backend.

## Project status
All 15 UI build steps are complete. Currently in **Step 15 — wiring to real backend**.
- Wired: auth, staff, activity log, permissions catalog, categories, products
- Partially wired (orders): list page wired to real backend (getOrders); artwork proof workflow (Step 4A–4I) — ProofUploadDialog, OrderProofs panel, proof API functions, proof hooks; order item normaliser updated for AdminOrderItemDetail snake_case shape + new fields (category_name, artwork_type, artwork_filename, template_data, turnaround_extra_cost); OrderItems.tsx rebuilt with category badge, spec pills, artwork section, and a separate turnaround surcharge line so per-unit price × quantity + surcharge = total is self-explanatory
- Partially wired (printing queue): `getPrintingQueueAll` (all four tabs incl. `ready_to_dispatch`, sourced from real `GET /admin/orders?status=` × 4 + per-order `GET /admin/orders/{id}`), `startPrinting`, and `markReadyToDispatch` wired to real backend (`PATCH /admin/orders/{id}/status`). `getServiceabilityBulk` and `createShipmentsBulk` now call the real Shiprocket-backed endpoints (see Shipping below). `approveArtwork`/`requestReupload` remain mock — still no admin-facing endpoint for staff-side artwork approve/reject (only customer-token/WhatsApp-driven `resolve_approval`). See `src/lib/api/printingQueue.ts` comments.
- Single-order dispatch rebuilt as a full page (was a cramped `BatchDispatchDialog` reused for both bulk and single-order dispatch, with two live bugs — see below): new route `/printing-queue/dispatch/[orderId]` (`src/app/(admin)/printing-queue/dispatch/[orderId]/page.tsx` → `DispatchOrderClient.tsx`). Shows order items (reuses `OrderDetail/OrderItems.tsx`), an editable weight/dimensions form (defaults 0.5kg/10×10×5cm, matching the backend placeholder), an editable delivery-address form pre-filled from `order.shipping_address` via RHF's `values` option, a "Check Couriers" action (`checkServiceability(pincode, weight_kg)`) rendering couriers as full-width radio cards (name/rate/day-range, no truncation), and a persistent "Enter Shipment Manually Instead" button (always rendered, not gated on any error state) that opens the existing `ManualShipmentDialog`. `QueueTable.tsx`'s per-row `ready_to_dispatch` "Create Shipment" button and the bulk-toolbar action (when exactly 1 row is selected) now `router.push` here instead of opening `BatchDispatchDialog`; the toolbar still opens `BatchDispatchDialog` for 2+ selected rows, and it remains the sole entry point for genuine multi-order dispatch. `createShipment(order_id, courier, overrides?)` in `src/lib/api/shipping.ts` gained an optional `overrides: { weight_kg?, length_cm?, breadth_cm?, height_cm?, address_override? }` param, sent as extra body fields on top of `{ courier }`; new `useCreateShipment()` hook in `src/features/shipping/hooks/useShipping.ts`. Backend's `CreateShipmentRequest`/`ShipmentAddressOverride` (`urgent-printers-backend/app/schemas/shipping.py`) confirmed to match these field names exactly, all-optional, backward compatible. **Bug fixes in `BatchDispatchDialog.tsx`** (kept for real bulk dispatch): "Enter manually" was only rendered when `entry?.error` was truthy, hiding the manual escape hatch whenever Shiprocket serviceability succeeded — now a persistent text-button rendered per-row regardless of serviceability outcome (only hidden once that row is confirmed dispatched). Courier `Select` widened from `w-36` trigger to `w-60` trigger / `SelectContent min-w-72`, with each item now a two-line block (full courier name, then rate · day-range on its own line) instead of one cramped truncating line.
- Wired (shipping): `getShipments` (`GET /admin/shipments`), `checkServiceability`/`getServiceabilityBulk` (`POST /admin/shipping/serviceability[/bulk]`), `createShipment`/`createShipmentsBulk` (`POST /admin/orders/{id}/shipment`, `POST /admin/shipments/bulk`, partial-failure tolerant). `Shipment` type renamed to match backend (`tracking_number`/`shipment_status`/`estimated_delivery_date`, `order_id` as row key, new `rto`/`cancelled` statuses). Courier rate is paise on the wire — converted to rupees in `src/lib/api/shipping.ts` before display. Couriers are now identified by `courier_id`, threaded through `BatchDispatchDialog`. Fixed a pre-existing bug in `orders.ts`'s address normaliser (`shippingAddress.pincode` → `.postal_code`, matching the backend field) that left `shipping_pincode` blank on printing-queue items. Manual courier fallback wired: `createManualShipment` (`POST /admin/orders/{id}/shipment/manual`, `shipping.manage`) via new `ManualShipmentDialog` (`src/features/shipping/components/`) — used from `OrderShipping.tsx` (order detail, shown when `shipping.courier` is empty and status is `ready_to_dispatch`) and as a per-row "Enter manually" escape hatch in `BatchDispatchDialog.tsx` when bulk serviceability errors for an order. `Shipment.shipment_source` (`'shiprocket' | 'manual' | null`) added and surfaced as a "Manual" badge in `ShipmentsTable.tsx`. Verified end-to-end in the browser: manual entry moves the order to `shipped`, populates the shipping card, and shows the Manual badge in the shipments list; existing Shiprocket bulk-dispatch happy path re-verified with no regressions.
- Consistency pass (manual vs Shiprocket across Orders/Printing Queue/Shipping, Step 15): extracted `SHIPMENT_STATUS_LABEL`/`SHIPMENT_STATUS_VARIANT` to `src/lib/constants/shipmentStatus.ts` (was duplicated inline in `ShipmentsTable.tsx`) so wording matches everywhere it's used. `ShipmentsTable.tsx`'s "AWB" column renamed "Tracking #" (and CSV export key `awb` → `tracking_number`) — misleading for manual rows, which are a free-text tracking number, not a real Shiprocket AWB. `PrintingQueueItem.awb_number` renamed `tracking_number` for the same reason (field is currently always null in practice — dispatched orders leave the printing-queue's tracked statuses (`REAL_QUEUE_STATUSES`) the instant a courier is assigned, since courier assignment and `status → shipped` happen atomically in both `create_shipment_for_order` and `create_manual_shipment` on the backend — so there's never a dispatched row left in the queue to mislabel; renamed anyway for when it's someday surfaced). `OrderShipping.tsx` (order detail) now renders the same "Manual" badge next to courier as `ShipmentsTable.tsx`, plus a shipment-status badge, plus a "Manually entered — no live tracking available" note when `tracking_url` is null and source is manual (mirrors the list's silent omission of the Track link, made explicit here since this card has more room) — **gated on a backend gap**: `GET /admin/orders/{id}`'s `AdminOrderShipping` schema (`urgent-printers-backend/app/schemas/order.py:316-322`, populated in `_build_order_detail`) doesn't return `shipment_status`/`shipment_source` even though `Order.shipment_status`/`Order.shipment_source` exist on the model — confirmed live against orders 129/130/343 via `GET /admin/orders/{id}`. `OrderShippingInfo` (`src/types/order.ts`) and `normaliseOrder` (`src/lib/api/orders.ts`) were updated to type and pass through these two fields now, defaulting to `null` until the backend adds them, so `OrderShipping.tsx`'s new badges/note activate automatically once it does — no frontend change needed then. `ShipmentStatus`/`ShipmentSource` type aliases moved from `src/types/index.ts` to `src/types/order.ts` (next to `OrderShippingInfo`) to avoid a circular import; `index.ts` re-exports them via the existing `export * from './order'` plus a local `import type` for use in `Shipment`. Checked and already fine, no changes: Orders list has no per-row shipment indicator at all (so nothing to be inconsistent — `AdminOrderListItem` doesn't carry `courier`/`shipment_source` either, only `tracking_number`); `OrderActions.tsx`'s cancel flow doesn't reference tracking fields; the Shipping screen has no status filter/stale-shipment alert and no row-click detail drawer, so there's no "manual sitting in `created` forever" or "broken live-tracking link" surface to fix there; `ManualShipmentDialog.tsx`'s copy already correctly says "no AWB is assigned"; null `tracking_url` was already handled without dead links in both `ShipmentsTable.tsx` and `OrderShipping.tsx`. **Required backend change (not made — outside this repo's scope)**: add `shipment_status: Optional[str]` and `shipment_source: Optional[str]` to `AdminOrderShipping` in `urgent-printers-backend/app/schemas/order.py`, populated from `order.shipment_status`/`order.shipment_source` in `_build_order_detail` (`urgent-printers-backend/app/api/v1/routes/admin/orders.py`).
- Partially wired (communications): WhatsApp templates wired — list, create (via Meta API), sync from Meta, message preview; notification templates and comm log still mock
- Wired (staff notification preferences, Phase 4): new page `/staff/notifications` (`src/app/(admin)/staff/notifications/page.tsx`) renders `StaffNotificationsMatrix` (`src/features/staff/components/`), a staff × channel (in_app/email/sms/push/whatsapp) grid for the `order.created` event, modelled on `RolePermissionsMatrix.tsx`'s table structure. `GET`/`PUT /admin/staff-notifications/preferences` wired via `src/lib/api/staffNotifications.ts` (`getStaffNotificationPreferences`, `upsertStaffNotificationPreference`); `useStaffNotificationPreferences`/`useToggleStaffNotificationPreference` in `src/features/staff/hooks/useStaffNotifications.ts` do optimistic toggle-with-rollback via TanStack Query `onMutate`/`onError`/`onSettled`. Adding a future event type is one entry in the `EVENT_TABS` array in `StaffNotificationsMatrix.tsx` — renders as a Tabs bar once there's more than one. SMS/WhatsApp switches are disabled (with an explanatory `title` tooltip) for staff without `phone_number` on file, matching the backend's `has_phone_number` flag and the 422 it returns otherwise; a note under the matrix links to the Staff page when any staff member lacks a phone. Gated behind `staff.manage` both in `SidebarNav.tsx` (nav item hidden) and inside `StaffNotificationsMatrix` itself (renders an "Access restricted" state instead of the table) via `usePermissions().canManageStaff`. Also added the previously-missing `phone_number` field to `StaffForm.tsx` (backend's `AdminStaffCreate`/`AdminStaffUpdate` already supported it) so there's actually somewhere to set it — `AdminUser`, `CreateStaffRequest`, `UpdateStaffRequest` types updated to match.
- Wired (dashboard + admin notifications bell): `src/lib/api/dashboard.ts` rewritten to call real `GET /admin/dashboard[, /revenue-chart, /recent-orders, /top-products, /order-status-breakdown]` — all money fields (`revenue_today`, `revenue_sparkline[]`, revenue-chart `value`, recent-orders `amount`, top-products `revenue`) arrive as Decimal-serialized JSON strings and are converted with `Number(...)` at the API boundary, same convention as `orders.ts`; `recent_orders`/`top_products` `id` (backend `int`) normalised to `string`. Hooks (`useDashboardStats.ts`) and components (`StatsRow`, `RevenueChart`, `OrderStatusBreakdown`, `RecentOrdersFeed`, `TopProducts`, `AlertsPanel`) needed no changes — shapes preserved. New bell-icon notification center: `src/lib/api/adminNotifications.ts` (`getAdminNotifications`/`markNotificationRead`/`markAllNotificationsRead`, wired to `GET/POST /admin/notifications[...]`, `id`/`admin_user_id` normalised to string), `src/features/notifications/hooks/useAdminNotifications.ts` (30s poll, optimistic mark-read via `onMutate`/`onError`/`onSettled`), `src/features/notifications/components/NotificationBell.tsx` (Base UI `DropdownMenu` off the bell in `AdminHeader.tsx`, unread badge, "mark all read", click-to-read + navigates to `ROUTES.ORDER_DETAIL` when `event_type` starts with `order.` and `data.order_id` is present). `AdminHeader.tsx`'s old alert-count bell (sourced from `stats.alerts`, a different concept — dashboard operational alerts, untouched, still feeds `AlertsPanel`) replaced entirely by `NotificationBell`. Note: Base UI's `Menu.GroupLabel` throws `MenuGroupRootContext is missing` outside a `<Menu.Group>` — don't use `DropdownMenuLabel` as a bare heading inside `DropdownMenuContent`, use a plain `<span>` instead. Verified end-to-end in the browser (Playwright + Chrome, logged in as the seeded super admin): dashboard renders real revenue/order/product/status-breakdown data (not the old `284500`/`Rahul Sharma` mock), bell shows correct unread badge count, dropdown lists a real notification with title/body/relative-time/unread dot, clicking it marks it read and navigates to `/orders/{id}`, "mark all read" and the empty state both confirmed.
- Wired (reports): `src/lib/api/reports.ts` rewritten to call real `GET /admin/reports/[sales, orders-analytics, customers-analytics, operations]` (note: `orders-analytics`, not `/reports/orders` — that's an unrelated pre-existing raw order list/CSV endpoint). All `ReportTimeSeriesPoint.value` fields (every `date`/`value` series — `revenue_over_time`, `avg_order_value_trend`, `orders_over_time`, `cancellation_rate_trend`, `fulfillment_time_trend`, `new_customers_over_time`, `artwork_reupload_trend`) plus `total_revenue`, `avg_order_value`, `revenue` (category/product), `amount` (payment method), `avg_lifetime_value`, `revenue` (customers by state) arrive as Decimal-serialized JSON strings and are converted with `Number(...)` at the API boundary, same convention as `dashboard.ts`/`orders.ts`. Hooks and report components needed no changes — shapes preserved exactly. Added 5 new date-range presets to `useReports.ts`: `today`/`yesterday`/`this_week`/`this_month`/`this_year` (via `date-fns` `startOfDay`/`endOfDay`/`startOfWeek`/etc.), alongside the existing `7d`/`30d`/`3m`/`1y`. Also fixed `useReportDateRange` to send full-precision ISO timestamps (`startOfDay(...).toISOString()` / `endOfDay(...).toISOString()`) instead of date-only strings — the old date-only format was parsed by the backend as midnight, which would've silently excluded same-day activity from every report and collapsed Today/Yesterday to a zero-width instant. `ReportChart.tsx` gained `isHourlySeries`/`formatChartDate` exports — the backend buckets ranges ≤2 days hourly with full ISO timestamps (not date-only), so `TrendChart`'s x-axis now shows time-of-day ("2 PM") instead of a repeated calendar date under Today/Yesterday; also fixed a latent bug in `CustomersReport.tsx`/`OperationsReport.tsx` where `d.date.slice(5)` (a mock-data-only shortcut assuming date-only strings) was replaced with the same formatter, since the real API always returns full ISO timestamps even at daily granularity. `ReportControls.tsx`'s period-pill row (`flex items-center gap-0.5`) was missing `flex-wrap` on the inner pill container — with 9 periods now it overflowed off-screen on mobile viewports (~390px) instead of wrapping like the outer row; added `flex-wrap` + `whitespace-nowrap` on the pill buttons. Verified end-to-end in the browser (Playwright + Chrome, logged in as the seeded super admin): all 4 report pages render real data (e.g. ₹24,364/7 orders/"Matte Finish Business Cards"/"Uttar Pradesh", not the mock's `4284500`/"Business Stationery"/Karnataka), all 9 period presets return 200s with no console errors across all 4 pages, Today/Yesterday render correctly-labeled hourly charts (not broken/empty-looking, just legitimately zero since no orders exist in seeded data for those two days), CSV export confirmed working, mobile pill-wrap fix confirmed visually.
- Wired (content): `src/lib/api/content.ts` rewritten to call real `GET/POST/PATCH/DELETE /admin/content/{banners,testimonials,faqs}` and `GET/PUT /admin/content/announcement`, all via `../urgent-printers-backend/app/api/v1/routes/admin/content.py`. List endpoints are paginated (`{items, page, page_size, total, total_pages}` folded by `client.ts`'s interceptor, same as `coupons.ts`) — fetched with `page_size=100` since none of the content managers paginate. Numeric `id` normalised to string at the API boundary for all four resources, same convention as `orders.ts`/`dashboard.ts`. `reorderBanners` posts to `/admin/content/banners/reorder` (`{ids: number[]}`); `AdminAnnouncementResponse` has no `valid_from`/`valid_until` (unlike banners) — `content.ts`'s `normalizeAnnouncement` always sets both to `null` since the frontend `Announcement` type still declares them; no UI depends on them being non-null.
- Wired (FAQ reorder, follow-up pass): `useFaqMutations()`'s reorder gap called out in the entry above is closed — `reorderFaqs(ids: string[])` added to `content.ts` (posts to `/admin/content/faqs/reorder`, identical shape to `reorderBanners`), `reorder` mutation added to `useFaqMutations()` in `useContent.ts`. `FaqsManager.tsx` renders FAQs grouped by category, not as one flat list, but `sort_order` is a single global field — so `move(id, dir)` operates on the full flat `allIds` array (`faqs.map(f => f.id)`, already sort_order-ordered from the backend) and always posts the complete reordered id list, regardless of which category group the arrows were clicked in; up/down disabled state is global-bounds (first/last in `allIds`), matching `BannersManager.tsx`'s convention exactly. Verified via curl against the running backend (seeded super admin): confirmed `GET /admin/content/faqs` returns items pre-sorted by `sort_order`, `POST /admin/content/faqs/reorder` with a full id list correctly reassigns `sort_order` by list position with no caching lag, then restored original order — no live browser session available this pass (no browser-automation tool), so this relied on typecheck (`tsc --noEmit`, clean) + lint (`eslint src/features/content/`, 0 errors, only pre-existing unrelated warnings) + curl-verified backend contract match. `useTestimonialMutations` still has no reorder mutation, even though `/admin/content/testimonials/reorder` also exists — out of scope for this pass. `BannerForm.tsx` gained a drag-and-drop image uploader (`react-dropzone` + `uploadMedia(file, 'banner', onProgress)`, same pattern as `ProductForm/MediaSection.tsx` but scaled to one image) above the existing `image_url` text field, which still works as a manual-paste fallback; on upload success it sets `image_url` to `variants.lg.url`. Verified against the running backend (seeded super admin, curl): banner create/patch/reorder/delete round-trip with the real DB (including a date-only `valid_from` string, e.g. `"2026-07-10"`, parsing correctly into a full datetime), testimonial/FAQ create+delete round-trip, announcement upsert confirmed against a pre-existing seeded row (update branch, not just create). **Security fix**: `BannersManager.tsx`/`TestimonialsManager.tsx`/`FaqsManager.tsx`/`AnnouncementBar.tsx` initially rendered all mutating controls (add/edit/delete/reorder) unconditionally, with no `content.manage` gate — a `content.view`-only staff member would see fully-enabled buttons that then 403'd on submit. Fixed by gating every mutating control behind `usePermissions().canManageContent` (`content.manage`), following the same hook/pattern as `StaffNotificationsMatrix.tsx`'s `canManageStaff` gate, but scoped to just the controls (not the whole view) since view-only users should still see the list/current state — an inline "You have view-only access to Content…" notice (matching `StaffNotificationsMatrix`'s Info-icon note styling) appears instead of a full-page block. `AnnouncementBar.tsx`'s form fields/switch/submit are wrapped in a `<fieldset disabled={!canManageContent}>` (plus an explicit `disabled` prop on the Base UI `Switch`, which doesn't inherit native fieldset disabling) rather than gated by conditional rendering, since it's a single persistent form, not a list. **Validation-feedback fix**: `TestimonialsManager.tsx`/`FaqsManager.tsx` had Zod schemas requiring `customer_name`/`content` and `question`/`answer` but never destructured `formState.errors`, so an invalid submit silently no-opped with zero user feedback — fixed by destructuring `errors` and rendering `{errors.<field> && <p className={cls.err}>...}` under each required input/textarea (added an `err` class to both files' local `cls` object, matching `BannerForm.tsx`'s `'mt-1 text-xs text-[var(--danger)]'`). `AnnouncementBar.tsx` had `errors` already destructured but only wired to `link_url`; added rendering for `message`/`bg_color`/`text_color`, and tightened `bg_color`/`text_color` Zod validation from bare `min(1)` to a hex-format regex (`/^#([0-9A-Fa-f]{3}|[0-9A-Fa-f]{6})$/`) so garbage color strings can no longer be saved and rendered as a broken inline style on the live storefront bar. `BannerForm.tsx`/`Banner` type gained `badge_text` (nullable, max 40 chars, optional in the Zod schema) — a small tag shown above the hero headline on the storefront; `content.ts`'s `toBannerPayload` already spreads `...data` generically, so no wiring change was needed there.
- Content bug fixes (post-wiring pass): **Announcement colour-picker bug** — `AnnouncementBar.tsx`'s `bg_color`/`text_color` each had TWO DOM inputs (a `type="color"` swatch and a plain hex text input) both bound via `{...register('bg_color')}`/`{...register('text_color')}`. Confirmed root cause by reading the installed `react-hook-form@7.74.0` source (`node_modules/react-hook-form/dist/index.cjs.js`): for any non-checkbox/radio field, `register`'s ref callback keeps only a single `_f.ref` — the second DOM node to mount (the plain text input, since it's the second element in that JSX pair) unconditionally overwrites the first's ref, and `setValue`/`reset` write the loaded/saved value with `_f.ref.value = n`, touching only that one canonical node. The `<input type="color">` swatch's ref is orphaned, so it never receives the value pushed by the `reset()` call in the `useEffect` that seeds the form on load (or by any `setValue`) — it sits at the browser's `#000000` default regardless of the real saved colour, while the paired hex text field shows the correct value. Typing in either input does still update RHF's internal value correctly (`onChange` is a plain prop, not ref-mediated) so the live preview looked fine, which is why the bug read as "the swatch doesn't reflect my saved colour / the two controls disagree" rather than "the form doesn't save." Fixed by dropping `register()` from both inputs and driving them as fully controlled elements off a single `watch()`/`setValue()` pair (the same pattern already used for `link_url`/`is_active` in this file), so the swatch and hex field always read from and write to the one reactive value. Verified via curl against the running backend (`PUT /admin/content/announcement` with a new `bg_color`/`text_color` pair, then `GET` to confirm persistence) that the payload shape the fixed form now sends round-trips correctly; a real browser click-through wasn't possible in that session (no browser-automation tool available), so this should get a quick manual click-test as a follow-up. **Testimonial avatar upload**: `TestimonialsManager.tsx`'s inline form gained an optional `avatar_url` field — small square dropzone (`react-dropzone` + `uploadMedia(file, 'testimonial', onProgress)`, reusing `BannerForm.tsx`'s upload pattern scaled down to a 56px circle) plus a plain URL text input as manual-paste fallback, matching `BannerForm.tsx`'s `image_url` dual-input convention. `avatar_url: z.string().optional()` (no `min(1)` — photo is optional) added to the Zod schema; backend's `AdminTestimonialCreate`/`Update` (`app/schemas/admin.py:449,464`) already accept it and only validate it's an absolute `http(s)://` URL when non-empty. The list row's avatar circle now renders `t.avatar_url` as an image when present, falling back to the existing initials badge. Verified via curl: `POST /admin/content/testimonials` with `avatar_url` set round-trips and persists; a second POST omitting it entirely still succeeds (confirms optional), both test rows deleted after. **Announcement countdown field**: backend added `countdown_end_at: Optional[datetime]` to `AdminAnnouncementResponse`/`AdminAnnouncementUpdate` (nullable, ISO datetime, display-only — doesn't affect visibility, which is still `is_active` alone). Added `countdown_end_at: string | null` to the `Announcement` type (`src/types/index.ts`); `content.ts`'s `normalizeAnnouncement` already passed it through unchanged via its `...raw` spread (only `valid_from`/`valid_until` are hardcoded null there, since those don't exist on the backend Announcement — comment added clarifying `countdown_end_at` is real and must not get swept into that null-out). `AnnouncementBar.tsx` gained a `datetime-local` "Countdown deadline (optional)" field (plain `register()`, single DOM node — not the old dual-register color-picker bug pattern), `countdown_end_at: z.string().optional()` in the Zod schema, `announcement.countdown_end_at?.slice(0, 16) ?? ''` in the `reset()` seed (datetime-local format, no timezone/seconds), and `new Date(values.countdown_end_at).toISOString()` (or `null` if blank) in the submit payload. **Announcement `{{timer}}` placeholder mechanism** (coordinated with a parallel storefront change in `urgent-printers-frontend`): the countdown is no longer implicitly appended whenever `countdown_end_at` is set — it now only renders where the admin explicitly types a literal `{{timer}}` token inside `message`; `countdown_end_at` with no `{{timer}}` in the message has no visible effect, matching the storefront's new rule (`hasPlaceholder = message.includes('{{timer}}')`, `active = countdown_end_at` set and in the future, placeholder swapped for the live countdown only when both hold, otherwise stripped with whitespace cleanup). `AnnouncementBar.tsx` gained: an "Insert {{timer}}" `size="xs" variant="outline"` `Button` next to the Message label that appends `" {{timer}}"` to the current message via `setValue(..., { shouldValidate: true, shouldDirty: true })`; helper text under Message and updated helper text under Countdown deadline explaining the two fields now work together; and a local `pad()`/`formatCountdown()`/`renderAnnouncementMessage()` trio mirroring the storefront's exact format string (`Xd : HHh : MMm : SSs` at ≥1 day, non-padded `Xh : MMm : SSs` under 1 day) driven by a `previewNow` state ticked every second via `setInterval` in a `useEffect` (cleared on unmount), so the live preview block now shows the actual rendered countdown/stripped-placeholder behaviour instead of the raw message string. No backend change needed — `countdown_end_at` and `message` were already independently persisted fields; this is purely a client-side interpretation change plus a UI affordance.
- Wired (content: nav links, Phase 6 of the nav-links roadmap): `GET/POST/PATCH/DELETE /admin/content/nav-links[/{id}]` and `POST /admin/content/nav-links/reorder` wired via new `src/lib/api/content.ts` section (`getNavLinks(placement)`, `createNavLink`, `updateNavLink`, `deleteNavLink`, `reorderNavLinks(placement, ids)` — reorder body is `{ placement, ids }`, unlike banners/faqs' unscoped `{ ids }`, matching the backend's per-placement validation). New `NavLink` type (`src/types/index.ts`) keeps `category_id` as a string FK reference (normalised from the backend's numeric id), not a computed URL string, so it stays live if the category is renamed/re-slugged. New hooks `useNavLinks(placement)`/`useNavLinkMutations(placement)` (`src/features/content/hooks/useContent.ts`) key their TanStack Query cache on `['nav-links', placement]` so header/footer lists never collide. New `src/features/content/components/NavLinksManager.tsx` renders separate Header/Footer tabs (Base UI `Tabs`), each an independent `PlacementLinksList` — reordering is structurally placement-scoped in the UI (no combined list), mirroring the backend's per-placement reorder validation. New `src/features/content/components/NavLinkTargetPicker.tsx`: a Category-vs-Custom-URL toggle that stores `category_id` (FK) or `custom_url` (validated via `LinkUrlPicker.tsx`'s exported `isValidLinkUrl`/`LINK_URL_INVALID_MESSAGE`, imported not redefined) — clears the other field on mode switch, enforcing the same one-or-the-other invariant as the backend's XOR validator, client-side, via `NavLinkForm.tsx`'s Zod `superRefine`. Delete handles the backend's 409 "last active link in placement" `ConflictError` with a specific toast (not a generic failure message) by checking `ApiError.status === 409`. Gated behind `content.manage` (mutating controls disabled/hidden, `content.view`-only sees an inline notice) from the first commit — same pattern as `BannersManager.tsx`, not deferred to a follow-up patch. New page `src/app/(admin)/content/navigation/page.tsx`; new sidebar entry (`Content > Navigation`, `Link2` icon, gated `content.view`) in `SidebarNav.tsx`; `ROUTES.CONTENT_NAVIGATION` added. Verified via `tsc --noEmit` (clean) and `next build` (clean, `/content/navigation` present in the route list); live curl/browser verification against the running backend wasn't performed this pass (blocked by sandbox policy around credential use) — contract correctness instead confirmed by direct reading of `urgent-printers-backend/app/api/v1/routes/admin/content.py:326-504` and `app/schemas/admin.py:561-648` against every field/path/status code used here.
- Wired (manual shipment status control): manual shipments (`shipment_source === 'manual'`) previously had no way to advance past `created`/`delivered` — closed via `PATCH /admin/orders/{id}/shipment/status` (`shipping.manage`, additive to the existing shipping routes), request `{ shipment_status: 'picked_up' | 'in_transit' | 'out_for_delivery' | 'delivered' | 'rto' | 'cancelled' }` (never `created` — creation-only). `updateShipmentStatus` added to `src/lib/api/shipping.ts`; `useUpdateShipmentStatus()` in `src/features/shipping/hooks/useShipping.ts` invalidates `shipments`/`order`/`orders`/`printing-queue`, same set as the other shipping mutations. New shared `src/features/shipping/components/ShipmentStatusControl.tsx` — a `Select` of the 6 target statuses, with a lightweight confirm `Dialog` gating the three terminal-ish values (`delivered`/`rto`/`cancelled`) since `delivered` also flips the order itself to delivered server-side; used in two places: `ShipmentsTable.tsx`'s status column (replaces the read-only badge with the control only when `shipment_source === 'manual'` and the viewer has `shipping.manage`; Shiprocket rows stay badge-only) and `OrderShipping.tsx` (order detail — a second row under the existing status badge, same gating). Backend 409s if attempted on a Shiprocket-sourced order (defense-in-depth only, not reachable from either surface since the control is never rendered for those rows) — surfaced via a specific toast on `ApiError.status === 409`, same pattern as `NavLinksManager.tsx`'s delete-conflict handling. Verified request/response shape by reading `urgent-printers-backend/app/api/v1/routes/admin/shipping.py:137-145` and `app/schemas/shipping.py:8-154` directly (built in parallel from the same spec) — no drift. `tsc --noEmit` and `next build` both clean; no browser-automation tool was available this pass, so no live click-through.
- Wired (payments list): `getPayments` in `src/lib/api/payments.ts` rewritten to call real `GET /admin/payments` (status/method/date_from/date_to/search/page/page_size passthrough, same param-building pattern as `orders.ts`'s `getOrders`). Numeric `id`/`order_id`/`customer_id` normalised to string; `amount` arrives as a Decimal-serialized JSON string (e.g. `"5500.00"`) and is converted with `Number(...)`, same convention as `orders.ts`/`dashboard.ts`. Backend's `PaymentStatus` enum has two values the frontend's `Payment.status` union doesn't — `awaiting_payment` and `partially_refunded` — mapped down via a `STATUS_MAP` in `payments.ts` to `pending` and `partial_refund` respectively so the existing `PaymentStatusBadge`/`Payment` type didn't need widening. `usePayments.ts` and `PaymentsTable.tsx` needed no changes. Verified against the running backend (seeded super admin, curl): response shape matches exactly (`{data: [...], message, meta: {page, page_size, total, total_pages}}`, folded by `client.ts`'s interceptor into `{items, ...meta}`), `status`/`method`/`search` filters all confirmed working with correct counts; `tsc --noEmit` clean, `eslint src/lib/api/payments.ts` 0 errors (2 pre-existing warnings on the untouched `getRefunds`/`createRefund` mock stubs). No live browser session available this pass (no browser-automation tool) — did not click through `/payments` in an actual browser, only confirmed the page route returns 200 and the API contract matches field-for-field via curl. `getRefunds`/`createRefund` (and the `/payments/refunds` screen) remain mock — no backend Refund model/endpoint yet, tracked in `v2/IDEAS.md`.
- Pending: orders (detail page full wiring), customers, coupons
- Wired (reviews): `getReviews`/`updateReviewStatus`/`replyToReview` in `src/lib/api/reviews.ts` now call the real `GET /admin/reviews`, `PATCH /admin/reviews/{id}/status`, `PATCH /admin/reviews/{id}/reply` endpoints (numeric ids/product_id/user_id/order_id normalised to strings at the API boundary). `Review.status` renamed from the mock's `'published'` to the backend's `'approved'` throughout (`ReviewsTable.tsx` status badge/filter, `useReviews.ts` mutation types) — no value-mapped shim, a straight rename per the payments-STATUS_MAP anti-pattern lesson. `Review.content` renamed to `body` (matches `AdminReviewListItem.body`); added `is_verified_purchase`, `helpful_count`; `customer_id` replaced with `user_id` to match the real payload. `ReviewsTable.tsx` approve/reject/reply actions are now gated behind `reviews.moderate` via `usePermissions().canModerateReviews` (that helper already existed in `src/hooks/usePermissions.ts` — just wasn't consumed here yet); view-only staff see a read-only table with an info notice, same pattern as `BannersManager.tsx`. `ReplyDialog.tsx` already called the real mutation hook, just needed the `content`→`body` field rename.
- Products hardening (backend `AdminProductCreate.pricing_tiers`/`.turnaround_options` changed to `Field(min_length=1)`, `AdminProductUpdate`'s stayed optional but now `min_length=1` when sent): audited `ProductForm/index.tsx` against both flows. `TurnaroundSection.tsx` is a fixed 3-row Standard/Express/Rush UI with no add/remove affordance and Standard forced `is_active`/disabled — structurally can never submit an empty array, confirmed safe as originally believed. `PricingSection.tsx`'s `useFieldArray` **could** be emptied to 0 rows with no client-side guard — `ProductForm/index.tsx`'s `save()` (called directly from the Publish/Save-as-Draft buttons via `onClick`, bypassing `form.handleSubmit`/the Zod resolver entirely — worth remembering next time a field needs validating here) now toasts "At least one pricing tier is required" / "...turnaround option is required" and aborts before the request if either array is empty. Also found and fixed a latent bug unrelated to the new constraint: `product.turnaround_options` for real seeded products can have fewer than 3 entries (e.g. `A5 Flyers`, id 13, has only Standard+Express, no Rush) — since `TurnaroundSection` always renders 3 rows regardless of array length, editing such a product left the 3rd row's `register()`'d inputs seeding themselves from empty/untyped DOM state instead of a real default. Fixed via new `normaliseTurnaroundOptions()`/`TURNAROUND_DEFAULTS` export in `TurnaroundSection.tsx`, applied to `product.turnaround_options` in the edit-mode `defaultValues` — pads to a complete, correctly-typed 3-tuple (missing slots get the same defaults `save()` inactive Rush) and forces Standard's `is_active: true` even if legacy data had it false. **`src/lib/api/client.ts` fix**: `toApiError()` typed `ApiError.message` as `string` but FastAPI's default `RequestValidationError` handler (any uncaught pydantic error, e.g. this `min_length=1`) returns `detail` as an array of `{loc, msg, type}` objects, not a string — was silently assigned as-is, so any 422 not raised via the app's own `AppError` classes would render as `[object Object]`-style garbage wherever `ApiError.message` is shown. Added `formatValidationDetail()` to flatten it to `"field: message"` text; applies app-wide, not just products. `ProductForm/index.tsx`'s catch block now shows `apiErr.message` for `status === 422` instead of a generic "Failed to save product". Verified via curl against the running backend (seeded super admin): confirmed the raw 422 shape for both fields, confirmed the exact create-flow default payload (5 tiers, 3 turnaround rows) round-trips with `201`, confirmed a normalised-turnaround PATCH round-trips with `200` for product 13, then reverted that product back to its original 2-entry state (test product created during verification was deleted after). No browser-automation tool available this pass — `tsc --noEmit`, `next build`, and `eslint` on all touched files are clean; the actual Save-button click-through in a live browser session is still unverified.
- Wired (content: site theme, Phase 7): new `GET/PUT /admin/theme` (`content.view`/`content.manage`) wired via `src/lib/api/theme.ts` (`getSiteTheme`, `updateSiteTheme(presetId)` — numeric `id`/`updated_by_admin_id` normalised to string, same convention as the rest of `content.ts`). New `SiteTheme`/`ThemePresetId` types in `src/types/index.ts`. New `src/features/content/components/ThemeManager.tsx` (single-row shape, modelled on `AnnouncementBar.tsx`) renders a 6-swatch grid — one clickable card per curated preset (`roseGold`/`gold`/`emerald`/`slate`/`pink`/`plum`), highlighting the active one and requiring an explicit Save (button disabled until the local selection actually differs from the server value). Preset labels/hex swatches live in new `src/lib/constants/themePresets.ts`, copied verbatim from `urgent-printers-frontend/lib/themes.ts`'s `THEMES` array (that file remains the source of truth — this is a display-only copy, not re-derived). Gated behind `content.manage` from the first commit, same pattern as `BannersManager.tsx`/`NavLinksManager.tsx`: swatches sit in a `<fieldset disabled={!canManageContent}>` (native buttons, so fieldset-disabling works without Base UI's `Switch`-style extra `disabled` prop workaround) and `content.view`-only staff see the same inline Info-icon read-only notice. Copy next to Save deliberately promises "within seconds" (not nav-links' up-to-5-minutes wording) since this feature's backend fires an instant cache-busting webhook rather than relying on a timed cache expiry. New page `src/app/(admin)/content/theme/page.tsx` (route `/content/theme`, singular — matches `navigation`'s convention, not the plural list-resource routes); new sidebar entry (`Content > Theme`, `Palette` icon, gated `content.view`) in `SidebarNav.tsx`; `ROUTES.CONTENT_THEME` added. Avoided a `react-hooks/set-state-in-effect` lint error by syncing local `selected` state from server data during render (tracked via a `syncedPresetId` guard state) instead of in a `useEffect` — the pattern `AnnouncementBar.tsx` uses (RHF's `reset()` in a `useEffect`) doesn't trip this rule since `reset` isn't a bare `useState` setter, but a raw `setSelected` call there does. `tsc --noEmit` and `next build` both clean, `/content/theme` present in the route list; no live backend/browser verification performed this pass per the no-unprompted-mutation rule (didn't call the real `PUT /admin/theme`) and no browser-automation tool was available.

## Stack
- Next.js 16.2.4 · React 19 · TypeScript strict
- Tailwind CSS v4 · shadcn 4.x with **Base UI** (not Radix)
- TanStack Query v5 · TanStack Table v8
- Zustand v5 (persist) · React Hook Form · Zod v4
- Tiptap v3 (rich text — `immediatelyRender: false`, uncontrolled via `defaultValue`)
- Motion (motion/react) · Recharts · Sonner · react-dropzone

## Directory structure
```
src/
  app/          — Next.js App Router (route segments)
  features/     — feature modules (orders/, staff/, products/, ...)
  components/   — shared UI (layout/, common/, ui/)
  hooks/        — shared hooks
  lib/
    api/        — all backend calls go here (client.ts + per-domain files)
    utils/      — helpers
    constants/  — roles, labels
  store/        — Zustand stores (authStore, sidebarStore)
  types/        — TypeScript types (index.ts + domain files)
```

## Auth
- Access token lives in memory only (Zustand, NOT in localStorage)
- `user` + `isAuthenticated` are persisted to localStorage via Zustand persist
- `_hasHydrated` flag in authStore — **AuthGuard and AuthLayout both wait for this** before acting (prevents page-reload race condition where localStorage hasn't loaded yet)
- 401 → refresh → retry is handled in `src/lib/api/client.ts` interceptor; auth endpoints are excluded from retry

## API conventions
- Base URL: `NEXT_PUBLIC_API_URL` (default `http://localhost:8000`)
- All calls go through `src/lib/api/client.ts` (`get`, `post`, `patch`, `del` helpers)
- Response envelope `{ data: T, message: string }` — client unwraps to `T`
- Errors: backend sends `{ detail }` or `{ message }` — both are mapped to `ApiError`
- Pagination: `{ items, total, offset, limit }`
- Numeric IDs from backend → normalised to strings at API boundary

## RBAC
- 6 roles; each user can have per-role grant/revoke overrides
- Permission catalog is fetched once per session: `GET /admin/permissions` → `useRoleCatalogue()`
- `getEffectivePermissions(user)` in `src/lib/utils/permissions.ts` — trusts backend's `user.permissions` when non-empty

## Critical Tailwind v4 pattern
```css
/* WRONG — @theme inline does not emit CSS variables */
@theme inline { --color-primary: var(--some-var); }

/* CORRECT — Tailwind writes --color-primary to :root AND generates bg-primary etc. */
@theme { --color-primary: oklch(0.38 0.16 271); }
.dark { --color-primary: oklch(0.55 0.18 271); }
```

## shadcn 4.x / Base UI gotchas
- No `asChild` on Trigger components — style them directly with `className`
- Button has no `asChild` — use raw `<Link>` with button classes. Always add `cursor-pointer` to button base styles.
- **DropdownMenuItem uses `onClick`, NOT `onSelect`** — `onSelect` is Radix-only and silently does nothing in Base UI
- Select `onValueChange` types value as `string | null` — coerce with `?? ''`
- Dark mode requires `@custom-variant dark (&:is(.dark *))` in globals.css — NOT media query

## Working rules
- Frontend-only role. If a backend change is needed, describe what endpoint/payload is required; don't touch backend files.
- Wire mock → real by editing only `src/lib/api/<domain>.ts`. Function signatures stay the same.
- No comments unless the WHY is non-obvious. No trailing summaries in responses.
